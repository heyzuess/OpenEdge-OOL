CLASS DATATYPE:
    DEFINE PUBLIC PROPERTY NAME  AS CHARACTER GET. PRIVATE SET.
    DEFINE PUBLIC PROPERTY VALUE AS CHARACTER GET. PRIVATE SET.

    CONSTRUCTOR DATATYPE(INPUT cip_char   AS CHARACTER):
        SUPER().
        ASSIGN THIS-OBJECT:NAME  = "CHARACTER"
               THIS-OBJECT:VALUE = cip_char.
    END CONSTRUCTOR.

    CONSTRUCTOR DATATYPE(INPUT lcip_char  AS LONGCHAR):
        SUPER().
        ASSIGN THIS-OBJECT:NAME  = "LONGCHAR"
               THIS-OBJECT:VALUE = STRING(lcip_char). 
    END CONSTRUCTOR.

    CONSTRUCTOR DATATYPE(INPUT iip_int    AS INTEGER):
        SUPER().
        ASSIGN THIS-OBJECT:NAME  = "INTEGER"
               THIS-OBJECT:VALUE = STRING(iip_int).
    END CONSTRUCTOR.

    CONSTRUCTOR DATATYPE(INPUT dip_dec    AS DECIMAL):
        SUPER().
        ASSIGN THIS-OBJECT:NAME  = "DECIMAL"
               THIS-OBJECT:VALUE = STRING(dip_dec).
    END CONSTRUCTOR.

    CONSTRUCTOR DATATYPE(INPUT dip_date   AS DATE):
        SUPER().
        ASSIGN THIS-OBJECT:NAME  = "DATE"
               THIS-OBJECT:VALUE = STRING(dip_date).
    END CONSTRUCTOR.

    CONSTRUCTOR DATATYPE(INPUT hip_handle AS HANDLE):
        SUPER().
        ASSIGN THIS-OBJECT:NAME  = "HANDLE"
               THIS-OBJECT:VALUE = STRING(hip_handle).
    END CONSTRUCTOR.

    CONSTRUCTOR DATATYPE(INPUT m_memptr   AS MEMPTR):
        SUPER().
        ASSIGN THIS-OBJECT:NAME  = "HANDLE"
               THIS-OBJECT:VALUE = STRING(m_memptr).
    END CONSTRUCTOR.

    DESTRUCTOR DATATYPE():
    END DESTRUCTOR.
END CLASS.